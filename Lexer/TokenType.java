
public enum TokenType {
    //Token-Class Resereved keywords
    MAIN, RETURN, NUM, TEXT, BEGIN, END, SEMICOLON, SKIP, HALT, PRINT, INPUT, LPAREN, RPAREN, COMMA, IF, THEN, ELSE, NOT, SQRT, OR, AND, EQ, GRT, ADD, SUB, MUL, DIV, VOID, LBRACE, RBRACE, EQUAL,
    //Token V
    V_NAMES,
    //Token-Class F
    F_NAMES,
    //Token-Class T
    TEXT_SNIPPET,
    //Token-Class N
    N_NUMBERS
}
